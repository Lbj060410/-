# 摄像头连接问题修复总结

## 修复内容

### 1. CameraDiscoveryService.ets - UDP 事件监听泄漏修复
**问题**: UDP 事件监听器未被移除，导致内存泄漏和连接异常
**修复**:
- 添加 `messageHandler` 变量追踪事件监听器
- 在超时和成功时都调用 `udp.off('message', messageHandler)` 移除监听
- 在 finally 块中确保监听器被清理
- 改进错误日志，显示具体错误信息

**影响**: 减少内存泄漏，提高 UDP 发现的稳定性

---

### 2. CameraHealthService.ets - 健康检查阈值优化
**问题**: 阈值过低导致频繁降级，网络抖动时频繁切换状态
**修复**:
- `degradedThreshold`: 5 → 10 (降级需要10次连续失败)
- `offlineThreshold`: 15 → 25 (离线需要25次连续失败)
- DEGRADED 状态间隔: 500ms → 800ms
- OFFLINE 状态间隔: 1200ms → 2000ms

**影响**: 减少误判，提高网络稳定性判断的准确性

---

### 3. CameraHttpService.ets - 超时时间调整
**问题**: 2.5秒超时对弱网络环境太短
**修复**:
- 超时时间: 2500ms → 4000ms

**影响**: 给弱网络环境更多时间完成请求，减少超时失败

---

### 4. HttpSnapshotService.ets - 客户端连接管理改进
**问题**: 客户端复用策略不当，导致连接状态混乱
**修复**:
- 添加 `lastUsedAtMs` 和 `clientReuseTimeoutMs` (30秒)
- 改进 `ensureClient()`: 检查客户端是否超时，超时则重建
- 在成功请求后更新 `lastUsedAtMs`
- 避免长时间复用同一连接导致的状态问题

**影响**: 更稳定的连接复用，减少连接状态异常

---

### 5. RtspHttpSwitchManager.ets - 切换逻辑优化
**问题**: 切换超时时间过长，容易频繁切换
**修复**:
- 添加 `lastSwitchAtMs` 和 `switchDebounceMs` (1000ms) 防抖
- RTSP 超时: 5000ms → 3000ms (更快响应)
- HTTP 陈旧判定: 3000ms → 2000ms (更敏感)
- 每次切换时更新 `lastSwitchAtMs` 防止频繁切换

**影响**: 更快的故障转移，同时避免频繁切换导致的卡顿

---

## 预期改进

1. **内存泄漏减少** - UDP 事件监听器正确清理
2. **网络稳定性提升** - 更合理的健康检查阈值
3. **弱网络支持更好** - 更长的超时时间和更智能的连接管理
4. **故障转移更快** - 更短的 RTSP 超时和防抖机制
5. **红绿灯识别速度提升** - 摄像头连接更稳定，帧率更稳定

## 测试建议

1. 在弱网络环境下测试摄像头连接稳定性
2. 观察红绿灯识别的帧率和延迟
3. 检查内存使用是否有改善
4. 验证 RTSP/HTTP 切换是否平滑

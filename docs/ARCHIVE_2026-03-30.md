# 2026-03-30 Cleanup Archive

## Final Runtime Path
- Keep local ncnn inference in app process (stable path).
- Keep video pipeline stability patches (adaptive timeout + fail backoff + soft restart).
- Disable/remove IPC and worker experimental paths for now.

## Removed Experimental Artifacts
- infer/ feature module scaffold.
- entry/src/main/ets/serviceextability/ (service extension prototype).
- entry/src/main/ets/services/recognition/NcnnIpcClient.ets.
- entry/src/main/ets/services/recognition/NcnnWorkerClient.ets.
- entry/src/main/ets/workers/.

## Notes
- Device-side profile parser rejected service extension packaging in this environment.
- If process-level isolation is needed later, prefer a separately signed app/service route validated against current device profile constraints.